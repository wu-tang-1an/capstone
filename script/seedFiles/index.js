const users = require('./users')
const tasks = require('./tasks')
const organizations = require('./organizations')
const columns = require('./columns')
const projects = require('./projects')
const commentsThroughTable = require('./commentsThroughTable')
const columnTasks = require('./columnTasks')
const orgProjects = require('./orgProjects')
const projectColumns = require('./projectColumns')
const userTasks = require('./userTasks')
const userOrgs = require('./userOrgs')

module.exports = {
  users,
  tasks,
  organizations,
  columns,
  projects,
  commentsThroughTable,
  columnTasks,
  orgProjects,
  projectColumns,
  userTasks,
  userOrgs,
}
